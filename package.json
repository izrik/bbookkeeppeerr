{
  "name": "bbookkeeppeerr",
  "version": "1.0.0",
  "description": "Bookkeeping and accounting system for businesses",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd packages/backend && uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd packages/frontend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd packages/frontend && npm run build",
    "build:backend": "cd packages/backend && uv build",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd packages/backend && uv run pytest",
    "test:frontend": "cd packages/frontend && npm run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}